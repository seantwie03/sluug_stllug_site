---
import BaseLayout from './BaseLayout.astro';
interface Props {
    presenter: string;
    lastUpdated: Date;
}
const { presenter, lastUpdated } = Astro.props;
---

<BaseLayout pageTitle={presenter}>
    Last Updated: {lastUpdated.toISOString().slice(0, 10)}
    <slot />
</BaseLayout>
